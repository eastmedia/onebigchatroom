<!DOCTYPE html>

<head>
  <title>onebigchatroom</title>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <link rel="shortcut icon" type="image/x-icon" href="favicon.ico" />
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application", "http://#{faye_host}/faye.js" %>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class="application">
    <header>
      <h1>One Big Chat Room<b></b></h1>
      <span class="tagline">autolysis hydrometer malt extract top-fermenting yeast, priming malt. krug beer filter fermentation, "dry hopping amber lager. units of bitterness keg." </span>
    </header>
    
    <%= yield %>
    
    <% if sidebar = yield(:sidebar) -%>
      <%= sidebar -%>
    <% end %>
  </div>


  <% if message_input = yield(:message_input) -%>
    <%= message_input -%>
  <% end %>

  <% if credits = yield(:credits) -%>
    <%= credits -%>
  <% end %>
  
  <%= render :partial => "shared/google_analytics" %>
</body>
</html>
